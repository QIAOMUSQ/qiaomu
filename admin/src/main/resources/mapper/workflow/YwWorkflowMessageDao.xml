<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiaomu.modules.workflow.dao.YwWorkflowMessageDao">
    <delete id="deleteByCommunity" parameterType="long">
        DELETE FROM yw_workflow_message WHERE community_id = #{communityId}
    </delete>


    <select id="getAll" resultType="com.qiaomu.modules.workflow.entity.YwWorkflowMessage">
        SELECT
            w.*, s.username,
            s.real_name,c.name as communityName
        FROM
          yw_workflow_message w
        LEFT JOIN sys_user s ON s.user_id = w.phone_one_id
        LEFT JOIN yw_community c on c.id = w.community_id
        <where>
            1=1
            <if test="communityId !=null and communityId!=''">
                and w.community_id = #{communityId}
            </if>
            <if test="dicValue !=null and dicValue!=''">
                and w.dic_value = #{dicValue}
            </if>
        </where>
    </select>

    <select id="selectById" resultType="com.qiaomu.modules.workflow.entity.YwWorkflowMessage" parameterType="long">
        SELECT
          w.*, s.username,
          s.real_name
        FROM
          yw_workflow_message w
        LEFT JOIN sys_user s ON s.user_id = w.phone_one_id
        WHERE w.id = #{id}

    </select>
</mapper>