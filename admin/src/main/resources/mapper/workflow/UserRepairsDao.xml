<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiaomu.modules.workflow.dao.UserRepairsDao">

    <select id="selectByRepairsId" resultType="com.qiaomu.modules.workflow.entity.UserRepairs">
        SELECT
            r.*, u.username AS repairsPhone,
            u.real_name AS repairsName
        FROM
            yw_user_repairs r
        LEFT JOIN sys_user u ON u.user_id = r.user_id
        WHERE r.repairs_id = #{repairsId}
    </select>
</mapper>