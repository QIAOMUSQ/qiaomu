<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiaomu.modules.sys.dao.SysUserDao">
	
	<!-- 查询用户的所有权限 -->
	<select id="queryAllPerms" resultType="string">
		select m.perms from sys_user_role ur 
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id 
			LEFT JOIN sys_menu m on rm.menu_id = m.menu_id 
		where ur.user_id = #{userId}
	</select>
	
	<!-- 查询用户的所有菜单ID --> 
	<select id="queryAllMenuId" resultType="long">
		select distinct rm.menu_id from sys_user_role ur 
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id 
		where ur.user_id = #{userId}
	</select>

	<select id="queryUserImageUrl" resultType="java.lang.String" parameterType="java.lang.String">
		select a.path  from sys_file as a
		JOIN sys_user  as b
		where   b.id=#{userId} AND b.hand_img_id = a.id
	</select>

	<select id="queryById" resultType="com.qiaomu.modules.sys.entity.SysUserEntity">
		SELECT * FROM sys_user WHERE user_id = #{userId}
	</select>

	<!--根据部门获取用户-->
	<select id="getLoginUser" resultType="com.qiaomu.modules.sys.entity.SysUserEntity">
		SELECT
			*
		FROM
			sys_user u
		WHERE u.dept_id = #{deptId};
	</select>

	<select id="getUserByUserName" resultType="com.qiaomu.modules.sys.entity.SysUserEntity" >
		SELECT
			*
		FROM
			sys_user
		WHERE username = #{userName};
	</select>
</mapper>