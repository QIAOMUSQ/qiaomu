<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiaomu.modules.sys.dao.UserExtendDao">


    <select id="getUserExtend" resultType="com.qiaomu.modules.sys.entity.UserExtend">
        SELECT * FROM yw_user_extend ue LEFT JOIN  sys_user u ON ue.user_id = u.user_id where u.username = #{userPhone}
    </select>

    <select id="selectAll" resultType="com.qiaomu.modules.sys.entity.UserExtend">
        SELECT * FROM yw_user_extend ue LEFT JOIN  sys_user u ON ue.user_id = u.user_id
        <where>
            <if test="userPhone != null  and userPhone !=''" >
               AND  u.username = #{userPhone}
            </if>
            <if test="companyId != null and companyId !=''">
                AND  ue.company_id = #{companyId}
            </if>
            <if test="communityId != null and communityId !=''">
                AND  ue.community_id = #{communityId}
            </if>
            <if test="userId != null and userId !=''">
                AND  ue.user_id = #{userId}
            </if>
        </where>
    </select>
    <select id="selectCommunityList" resultType="com.qiaomu.modules.sys.entity.UserExtend">

        SELECT * FROM yw_user_extend
        <where>
            <if test="userId != null  and userId !=''" >
                AND  user_id = #{userId}
            </if>
            <if test="companyId != null and companyId !=''">
                AND  company_id = #{companyId}
            </if>
            <if test="communityId != null and communityId !=''">
                AND  community_id = #{communityId}
            </if>
            <if test="check != null  and check !=''" >
                AND  check = #{check}
            </if>
            <if test="status != null  and status !=''" >
                AND  status = #{status}
            </if>
            <if test="companyRoleType != null  and companyRoleType !=''" >
                AND  company_role_type = #{companyRoleType}
            </if>
        </where>

    </select>

</mapper>