<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiaomu.push.dao.UserDao">

    <select id="findUserByCommunityId" resultType="com.qiaomu.push.entity.User" parameterType="long">
        SELECT
            u.user_id,u.username,u.client_id,e.community_id,e.company_role_type as communityRoleType
        FROM
            sys_user u
        LEFT JOIN yw_user_extend e ON u.user_id = e.user_id WHERE community_id = #{communityId}
    </select>
</mapper>